<!DOCTYPE html>
<html>
  <head><title><%= title %></title></head>
  <body>
    <h1><%= title %></h1>
    <p>TODO: build this view.</p>
    <pre><%= JSON.stringify(categories || items || item || category || {}, null, 2) %></pre>
  </body>
</html>
